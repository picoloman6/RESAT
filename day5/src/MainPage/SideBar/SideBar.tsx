import { SideBarWapper, SideBarModal } from './Sidebar.style';

const SideBar = () => {
  return (
    <SideBarWapper>
      <SideBarModal>여기</SideBarModal>
    </SideBarWapper>
  );
};

export default SideBar;
